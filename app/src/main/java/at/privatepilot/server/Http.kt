package at.privatepilot.server


class Http(port: Int) {
/*    init {
        embeddedServer(Netty, port) {
            install(ContentNegotiation) {
                jackson {}
            }

            routing {
                // Define your HTTP routes here

                get("/") {
                    call.respondText("Hello, this is the HTTP server!")
                }
            }
        }.start(wait = true)
    }*/
}